#include<stdio.h>
int main(){
	int a,b;
	printf ("nhap 2 so a,b tu ban phim");
	scanf ("%d%d",&a, &b);
	     if (a>b){
		 
	     printf ("so lon hon=  %d\n",a);
	 }
	      else{
	      	if(a<b);
	      	printf("so lon hon =  %d\n",b);
		  }
	    
	
	  
	return 0;
}
